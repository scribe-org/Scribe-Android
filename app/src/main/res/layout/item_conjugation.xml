<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp"
    app:cardBackgroundColor="@color/conjugation_card_background"
    app:cardCornerRadius="12dp"
    app:cardElevation="3dp"
    app:strokeColor="@color/conjugation_card_stroke"
    app:strokeWidth="1dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingHorizontal="16dp"
        android:paddingVertical="16dp">

        <TextView
            android:id="@+id/tense_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:letterSpacing="0.02"
            android:textAllCaps="false"
            android:textColor="@color/conjugation_text_secondary"
            android:textSize="14sp"
            tools:text="Past" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/person_text"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:textColor="@color/conjugation_text_secondary"
                android:textSize="15sp"
                tools:text="he/she/it" />

            <ImageButton
                android:id="@+id/copy_form_button"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="@string/app_conjugate_copy"
                android:padding="12dp"
                android:src="@drawable/ic_content_copy_outline"
                android:tint="@color/conjugation_text_secondary"
                android:tintMode="src_in" />
        </LinearLayout>

        <TextView
            android:id="@+id/form_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:textColor="@color/conjugation_text_primary"
            android:textSize="22sp"
            android:textStyle="bold"
            tools:text="worked" />
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
